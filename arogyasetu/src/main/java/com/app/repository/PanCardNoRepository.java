package com.app.repository;

import com.app.model.PancardNo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PanCardNoRepository extends JpaRepository<PancardNo,Integer> {
}
